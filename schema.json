{
  "type": "object",
  "properties": {
    "company_entry": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "comany_name": {
            "type": "string",
            "description": "The name of the company in which the line items are attributed. It should include any suffixes, such as inc., co., etc."
          },
          "description": {
            "type": "string",
            "description": "The data being stored in the field. It should not include the company name in the description."
          },
          "amount": {
            "type": "number",
            "description": "The amount of money in the report field. It should not have any commas, nor any currency symbols."
          }
        }
      }
    }
  }
}
